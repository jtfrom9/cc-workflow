# cc-workflow — 対話・作業ルール

このファイルは `hooks/session-start.sh` 経由で `additionalContext` として
セッション開始時に Claude のコンテキストに注入されます。

## 対話

### 質問への優先回答

ユーザの指示文に疑問系の文（`?` で終わる文、「〜できる？」「〜だよね？」のような修辞疑問を含む）が含まれている場合、**先に疑問にだけ答え、ユーザの次の合図を待つ**。同時に含まれる命令文は、その時点では実行しないこと。

例: 「X を実装して。ところで Y は可能？」 → 先に Y についての回答だけ返す。X の実装には着手しない。

### 指摘への対応

ユーザから **技術的・事実関係に関わる指摘**（実装が仕様と違う／ファイル名が誤っている／API の挙動が違う等）を受けたとき:

1. 自分の根拠（コード・ドキュメント等）に立ち返って **再検証する**
2. 必要に応じて、ユーザにも根拠や期待する挙動を確認する
3. 再検証で自分が誤っていれば、その時点で訂正
4. 再検証して自分が正しいと確認できたら、根拠を明示して説明する。根拠なしに正しい分析を撤回しない
5. ただしユーザが「仕様を変更する」「方針を変える」と明確に意思表明した場合は、それに従う

文体・冗長さ・トーンに関する指摘（事実関係ではないもの）は、再検証せず素直に従う。

## 実装

### コメント言語

ソースコード（`.py` / `.sh` / `.ts` / `.js` / `.go` / `.rs` / `.c` / `.h` / `.hpp` / `.cpp` / `.cs` 等）の **コメント・docstring は英語で書く**。プロジェクトの CLAUDE.md やコーディング規約で日本語が明示指定されている場合のみ日本語。

Markdown / README / 設計ドキュメントの本文は対象外（日本語のまま）。

### ブランチ作成時の確認

`git checkout -b` 等で新規ブランチを切る作業（PR 作成を含む）の前に、以下を確認する:

- **ベースブランチ**（新ブランチを切る起点）
- **マージ先ブランチ**（PR の統合先）

両者は別になり得る（例: `develop` から切って `main` にマージ）。文脈から明確に推測できる場合は、その推測を明示しつつ確認なしで進めて良い。文脈で曖昧な場合は必ず聞く。

### バグ修正・機能追加・リファクタリングは t_wada 流 TDD で進める

原則として Red → Green → Refactor のサイクル:

1. **Red**: 期待する挙動を表現する **失敗するテスト** を先に書く
2. **Green**: そのテストを通す **最小限の実装** を行う
3. **Refactor**: テストを通したまま実装を整える

対象別:

- **バグ修正** → 再現できる失敗テストを書いてから直す
- **機能追加・改修** → 新仕様のテストを書いてから実装
- **リファクタリング** → 既存テストを **変更せず** 通したまま実装を変える（テスト自体を変えないのがリファクタの定義）

例外（TDD を要しない）:

- typo 修正、ログ消し、コメントだけの変更
- 機能を変えない見た目調整（色、マージン、文字サイズ等）
- ユーザが明示的に「TDD スキップで」「軽く直すだけで」と指示した場合

既存テスト基盤が無いプロジェクトでは **テスト基盤の整備から始める**。基盤作成自体が大仕事になる場合は、TDD をスキップしてよいかをユーザに相談する。
